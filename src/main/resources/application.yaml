spring:
  cloud:
    gateway:
      default-filters:
        - TokenRelay
      routes:
        - id: product-resource-server
          uri: http://localhost:9000
          predicates:
            - Path=/**
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://keycloak:8080/auth/realms/biro/protocol/openid-connect/certs
      client:
        provider:
          my-keycloak-provider:
            issuer-uri: http://keycloak:8080/auth/realms/biro
            # Individual properties can also be provided this way
            #            token-uri: http://localhost:8080/auth/realms/amrutrealm/protocol/openid-connect/token
            #            authorization-uri: http://localhost:8080/auth/realms/biro/protocol/openid-connect/auth
            #            userinfo-uri: http://localhost:8080/auth/realms/amrutrealm/protocol/openid-connect/userinfo
            #            user-name-attribute: preferred_username
        registration:
          keycloak-spring-gateway-client:
            provider: my-keycloak-provider
#            scope: openid
            client-id: spring-gateway-application
            client-secret: b8cba1c6-faf0-4104-81ba-b6748d7aacd9
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/keycloak"

# Enable these to debug the Authorization flow
#logging:
#  level:
#    org:
#      springframework:
#        security:
#          web: DEBUG

server:
  port: 9090

management:
  endpoints:
    web:
      exposure:
        include: "*"